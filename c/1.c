#include<stdio.h>
int main() {
    int n1, n2;
    printf("Enter the number 1: ");
    scanf("%d", &n1);
    printf("Enter the number 2: ");
    scanf("%d", &n2);
    if(n1 > n2)
        printf("%d is larger than %d", n1, n2);
    else
        printf("%d is larger than %d", n1, n2);
    printf("\n");
}